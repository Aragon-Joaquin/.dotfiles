this is using kickstart.nvim as a starter template

# things that you should know:

do :checkhealth after all the plugins, you might to install packages as:

- go
- node
- python
- ripgrep, fzf
- kitty/ghostty (optional)
- and probably another core package such as gcc or cargo for compiling, 
- base-devel package (which i did NOT have it)
